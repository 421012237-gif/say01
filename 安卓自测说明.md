# 十一说 Android V2.7.0 一步流版

这是供本人手机测试的 Android Debug APK，不是 Google Play 正式发布包。

- 安装包：`ElevenSays-Android-V2.7.0-One-Move.apk`
- App ID：`com.say01.english`
- 版本：`2.7.0`（versionCode `20700`）
- 文件大小：141,744,257 字节（约 135.2MB）
- SHA-256：`8dc43ea27474a31da1974d41247b371e0dd55b195eed1c5adeac06c1a75b5aae`
- 系统：Android 7.0 及以上、64 位 ARM 手机
- 私测发布：GitHub 草稿 Release `v2.7.0-private-test-20260826`，不公开

## 安装

本包与 V2.5.0 使用同一签名证书，可以直接覆盖安装并保留学习进度。不要先卸载旧版；卸载会清除 App 内的本机记录。为保险，安装前可先在旧版“我的”导出进度。

1. 用安卓手机打开本次临时下载链接，下载约 135MB APK。
2. 下载完成后打开 APK；若系统拦截，只对当前浏览器临时允许“安装未知应用”。
3. 系统应显示“是否要更新此应用”，选择更新。
4. 安装后建议关闭浏览器的“安装未知应用”权限。
5. 打开 App，在“我的”确认版本为 V2.7.0。

## 最短发布闸门

只需先完成下面 6 项，并把结果告诉 Codex：

1. 覆盖安装成功，旧进度仍在。
2. 打开第一课，初始只有“听一句”；对方台词听完只出现选意思，答对后自动下一句。
3. 到自己的台词，听完只出现跟说判断；正常说一次，手机只显示第一条识别结果，并给“听懂了 / 能听懂 / 差一个关键点 / 没听完整”之一。
4. 跟读通过后自动下一句，不再当天强制脱稿；直接跳过不能增加掌握，脱稿应在次日到期复习回来。
5. 进入 `AI LIVE`，发送 `A latte, please.`，确认百炼返回下一句；再点朗读，确认是本地少女声而不是系统男声。
6. 进入“我的”运行诊断并导出进度，确认关闭重开后记录仍在。

## 建议的误判测试

- App 正在播放声音时立刻点麦克风：应先停止播放，再开始听你说。
- 故意漏掉 `a / the / please`：不应一律判完全错误。
- 故意换掉关键词：不应通过，只提示一个最值得重练的词。
- 在安静环境完全不说：低置信度或空结果不能算通过。
- 拒绝麦克风权限：固定课程仍能继续，并可从提示进入系统设置恢复权限。

## 自动验证已经证明什么

- GitHub Actions 运行 `32994014907` 完成 Android 21/Gradle 云端构建。
- 品牌、AI 客户端与中转、60 条音频、第一候选语音判断、播放停止、能力阶段、迁移、诊断、备份和 AI 每日上限自动检查通过。
- APK v2 签名验证通过；构建前密钥与构建后 APK 证书均与已安装 V2.5.0 的 SHA-256 证书指纹一致。
- GitHub 草稿资产大小和 SHA-256 digest 已由服务端返回，未公开发布。
- 北京函数真实健康检查和 `qwen3.7-plus` 对话均返回 200。

这些证据不能替代你的真机麦克风、动态声音和完整流程体验。完成上面的 6 项前，不称为“安卓全面测试通过”。

## 安全与费用边界

百炼长期 API Key 只在阿里云函数环境变量，不在 APK、网页或源码中。APK 内只有可撤销受限口令，仍可能被有能力的人提取，所以不要公开转发 APK 或临时下载链接。

固定课程和本地少女声不消耗百炼额度。AI 文字使用个人百炼额度；App 本机每日最多 20 个成功回合，云端默认单实例每日 60 次 / 120000 Token，但这不是阿里云账号级硬预算，也不能承诺 AI 永久零费用。

跟读判断使用安卓系统已有的英语识别服务，部分厂商手机可能联网或没有可用识别服务。它判断的是“交流是否可能听懂”，不是音素级发音考试，也不提供虚假的百分制。
