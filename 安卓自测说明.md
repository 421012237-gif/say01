# 十一说 Android V2.4.2 百炼 AI LIVE 自测版

这是供本人手机测试的 Android Debug APK，不是 Google Play 正式发布包。支持 Android 7.0 及以上系统。

- 计划安装包：`ElevenSays-Android-V2.4.2-百炼AI陪练版.apk`
- App ID：`com.say01.english`
- 版本：`2.4.2`（versionCode 20402）
- 文件大小：等待新 APK 构建后填写
- SHA-256：等待新 APK 构建并验签后填写

## 安装

1. 新 APK 完成云端构建、验签和包内反查后，把 `ElevenSays-Android-V2.4.2-百炼AI陪练版.apk` 下载到安卓手机。
2. 点击 APK；如果系统阻止安装，只对当前文件管理器或浏览器临时允许“安装未知应用”。
3. 安装完成后，关闭该权限。
4. 打开桌面上的粉黑 `十一说` 图标；图标主视觉是 `11`。
5. 进入咖啡店场景，按页面顺序“1 先听 → 2 跟说 → 3 下一句”。
6. 点击第一屏粉色的“点这里，先听一遍”，同时按手机侧边音量＋确认媒体音量。

如果手机提示“应用未安装”或“签名不一致”，先在旧版“我的”中导出需要保留的进度，再卸载旧的 Debug 自测版并安装 V2.4.2。不同云端 Debug 构建的测试签名可能无法直接覆盖；卸载会清除旧版 App 内进度。应用 ID 仍保留为 `com.say01.english`，签名一致时可直接覆盖并继续读取旧进度。

## 数据与权限

- 课程、30 条甜暖少女声整句、30 条同声线拼写关键词和界面已经打包进 APK。
- V2.4.2 在 Android App 内优先使用原生 `MediaPlayer` 播放固定整句和关键词；百炼动态少女声通过安全中转返回，失败时只显示文字，不回退系统男声。
- 问句、感谢、回应、求助和拼写词分别调整语速与音高；它仍是合成音，不冒充真人外教。
- App 不需要账号或订阅；安装后核心课程和静态音频无需联网。
- 学习进度保存在 App 自己的本机空间，不与浏览器网页版自动共用。
- 可以先在网页版“我的”中导出进度，再在 App 中导入。
- 只有点击录音或可选语音识别时，才应出现麦克风权限请求。
- 拒绝麦克风权限不应影响课程、拼写、听音和普通复习。
- 卸载 App 或在系统设置中“清除数据”会删除 App 内进度；重要进度先导出备份。

## 第一轮核心测试

- [ ] 能安装并从桌面粉黑图标打开，没有白屏或横向溢出
- [ ] 进入课程后第一屏能看见“1 先听 / 2 跟说 / 3 下一句”和粉色播放按钮
- [ ] 点击粉色“点这里，先听一遍”能听到示范，页面显示“正在播放”再变为“播放完成”
- [ ] 在“听声音，补回消失的一块”点击圆形“听词”，能听到关键词，页面显示“正在播放”再变为“听到了”
- [ ] 在复习里的听力题和拼写题也能听到同一套少女声
- [ ] 6 个场景能打开，静态示范音频在飞行模式下仍能播放
- [ ] 完成一句拼写补全后，关闭并重开 App，记录仍存在
- [ ] 第一次点击录音才请求麦克风；允许后能录制和回放
- [ ] 拒绝麦克风后，听音、拼写和复习仍可继续
- [ ] 一组角色扮演能走到结束并回放
- [ ] JSON 进度能导出，再从文件选择器导入
- [ ] “加到日历”生成的提醒文件能被手机日历打开
- [ ] AI LIVE 连接页只要求 HTTPS 中转地址和可撤销访问口令，没有百炼长期 API Key 输入框
- [ ] 连接真实中转后，咖啡场景至少完成 3 个动态回合，纠错一次最多一处
- [ ] 开场和 AI 动态回复能听见 Mia 女声；自动播放被拦截时，点击回复下方按钮可以播放
- [ ] TTS 暂不可用时明确显示降级，但文字对话、固定少女声和学习记录不受影响
- [ ] 完成一轮后记忆句进入 AI 回声，关闭重开 App 后仍存在

## 自测边界

- Debug APK 使用开发调试签名，仅供当前自测，不用于公开商店分发。
- 安卓系统可能提示“未知来源”或“未验证开发者”；确认文件来自本项目后再安装。
- V2.3.1 真机复测已确认整句能响；随后发现拼写区仍无声，且原女声音色偏冷、缺少情绪。V2.3.2 已把课程与复习中的 30 个拼写关键词也切到原生离线音频，并重做 28 条固定整句。
- 浏览器交互测试已确认整句与关键词都能从“正在播放”走到“播放完成/听到了”；最终仍需在同一台安卓手机确认音量和主观声音感受。
- V2.3.3 的60条固定少女声音频曾通过格式、覆盖、云端构建、v2签名和包内反查；V2.4.2 继续沿用这些资源，但新 APK 仍必须重新验证，旧证据不能替代新包验收。
- 百炼客户端与中转已通过模拟 `qwen3.7-plus`、严格 JSON Schema、鉴权、限频和 TTS 音频回传测试；尚未用真实百炼 Key 调用，不能据此宣称真实回复、声音或延迟已经合格。
- 尚未在真实安卓手机上完成麦克风、MediaRecorder、可选语音识别、日历下载和文件导入导出复测。
- 部分 Android WebView 不提供浏览器语音识别；它是可选功能，不影响核心学习、录音和复习。
