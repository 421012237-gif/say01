# SAY/01 AI LIVE｜个人免费自测说明

V2.4.0 的 AI 不是一个独立聊天框。它接在每个课程场景的最后：AI 扮演店员、当地人、新朋友、店员或同事，你用一句简单英语回答，它根据你的意思自然接下一句。

## 第一次连接

1. 打开 <https://aistudio.google.com/app/apikey>，登录自己的 Google 账号。
2. 创建一枚 Gemini API 密钥。Google 当前为部分模型提供免费层，但额度、地区与条款以后可能调整。
3. 回到 SAY/01，进入任意场景，点最下方荧光粉边框的 `AI LIVE`。
4. 粘贴密钥，阅读并勾选联网与数据说明，点“保存连接 · 进入场景”。
5. 以后可以在“我的 → AI 陪练连接”更换或删除本机密钥。

密钥只保存在当前网页或 Android App 自己的本机存储，不会写入代码、APK、GitHub 仓库或导出的学习进度。它仍然属于敏感凭据：不要截图发送给别人，不要在共享手机上保存。这个直连方式只适合本人自测；正式给别人使用时，应把密钥放到服务端安全代理中。

## 怎么练

- 进入已经学过或正在学的场景，点 `AI LIVE`。
- 每回合只说一句。不会完整句时，先说两三个词也可以。
- 可以打字；设备支持时也可点“说给 AI 听”，先确认识别文字，再发送。
- AI 每次只指出一处最值得改的地方。能听懂时不会为了挑错而硬讲语法。
- 想不到下一句时点“给我一点提示”；提示默认隐藏，先给自己一次主动回想的机会。
- 一轮最多 8 回合。结束后系统自动挑一句放进“复习 → AI 回声”，按照 1、3、7、14、30 天逐步再遇见。
- 旧的 AI 记忆句也会在后续场景中作为背景目标，让 AI 在合适的时候自然把它带回来。

## 声音边界

固定课程、拼写词和原角色扮演继续使用随 App 打包的少女声。AI 动态回复目前只显示文字，不调用手机系统朗读，因此不会突然变成男声。语音输入只是把你说的话转成文字，不是发音评分。

如果后续接动态少女声，需要选定可试听、可控情绪且费用明确的语音服务，并通过服务端保护密钥；不能为了“先有声音”又退回不可控的系统音色。

## 隐私和费用

- AI 模式需要联网，会把本轮对话文字发送给 Google Gemini，或发送到你自己填写的安全代理。
- Gemini 免费层的内容可能被用于改进 Google 产品；不要输入身份证、地址、账号、公司秘密或其他敏感信息。
- 基础课程永久免费且可离线使用。AI 免费层由第三方提供，不承诺永久不限量；遇到额度限制时，基础学习、少女声、录音、拼写和复习不受影响。
- 免费层与付费层的当前规则以 Google 官方页面为准：<https://ai.google.dev/gemini-api/docs/pricing>。
