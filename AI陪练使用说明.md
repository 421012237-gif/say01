# 十一说 V2.5.0 AI LIVE｜百炼个人自测说明

AI 不是一个独立聊天框。它接在每个课程场景的最后：AI 扮演店员、当地人、新朋友或同事，你用一句简单英语回答，它根据你的意思自然接下一句。

文字模型使用阿里云百炼北京地域 `qwen3.7-plus`。AI 英文回复由 APK 内置 `Kokoro-82M INT8 / af_sky` 在手机本地朗读，不调用百炼语音，也不回退手机系统男声。百炼长期 API Key 不进入网页或 APK。

## 第一次使用

1. 覆盖安装 V2.5.0 私人自测 APK，不要先卸载旧版。
2. 打开任意学习场景，进入末尾的荧光粉 `AI LIVE`。
3. 首次阅读并确认联网说明；不需要填写中转地址、访问口令或百炼 API Key。
4. 输入或说一句简单英语，发送后等待 `qwen3.7-plus` 接下一句。
5. 回复出现后点朗读按钮，手机会用本地 `af_sky` 少女声生成并播放。

手机只带有中转地址和可撤销受限口令，不包含百炼长期 API Key；连接信息不会进入学习进度备份。受限口令理论上可从 APK 中提取，因此此包只供本人测试，不要转发或公开发布。

## 怎么练

- 进入已经学过或正在学的场景，点 `AI LIVE`。
- 每回合只说一句。不会完整句时，先说两三个词也可以。
- 可以打字；设备支持时也可点“说给 AI 听”，先确认识别文字，再发送。
- AI 每次只指出一处最值得改的地方。能听懂时不会为了挑错而硬讲语法。
- 想不到下一句时点“给我一点提示”；提示默认隐藏，先给自己一次主动回想的机会。
- 一轮最多 8 回合。结束后系统自动挑一句放进“复习 → AI 回声”，按照 1、3、7、14、30 天逐步再遇见。
- 旧的 AI 记忆句也会在后续场景中作为背景目标，让 AI 在合适的时候自然把它带回来。

## 声音

- 固定课程、拼写词和原角色扮演继续使用随 App 打包的少女声，不依赖百炼。
- AI 开场和动态回复都在 Android 手机本地生成少女声，首次加载约 128MB 模型会比后续慢。
- 本地语音失败时仍保留文字，不调用手机系统语音，因此不会回退成不可控男声。
- 当前音色是合成音，不是真人或外教录音；“甜、自然、有情绪”仍需用户在同一台真机试听后判断。

## 隐私和费用

- AI 模式需要联网。本轮文字会经自己的中转发送给阿里云百炼；动态语音只在手机本地处理，不发送给百炼语音服务。
- 中转只记录模型、Token/字符数量和错误码，不记录用户对话原文；手机学习状态仍保存在本机。
- 不要输入身份证、地址、账号、公司秘密或其他个人敏感信息。
- 基础课程永久免费且可离线使用。百炼新人额度有期限，模型与语音服务之后可能按量计费，不能承诺“永久免费无限 AI”。
- 文字价格、免费额度和函数计算费用以阿里云控制台为准，并应设置预算与费用告警。本地语音不消耗百炼语音额度。

## 已验证与仍需真机验证

- 已用用户自己的百炼 Key 通过独立中转真实调用 `qwen3.7-plus`：无口令返回 401、伪造来源返回 403、正确请求返回 200，并取得符合约束的英文回复、中文理解、反馈、提示和记忆句。
- Android APK 已完成版本、签名、压缩、资源、预置中转和长期 Key 形态检查；Android WebView 的真实联网、本地动态朗读、麦克风听写与 8 回合完整流程仍需在用户手机验收。
- 100 万 Token 等新人额度并非永久免费承诺，且语音额度与文字额度不能混为一项。
