# SAY/01 AI LIVE｜百炼个人自测说明

V2.4.1 的 AI 不是一个独立聊天框。它接在每个课程场景的最后：AI 扮演店员、当地人、新朋友或同事，你用一句简单英语回答，它根据你的意思自然接下一句。

文字模型已换为阿里云百炼 `qwen3.7-plus`；动态回复可由 `qwen-audio-3.0-tts-flash` 使用 Mia 女声音色朗读。两者都必须经过自己的安全中转，百炼长期 API Key 不进入网页或 APK。

## 第一次连接

1. 在阿里云百炼北京地域创建 API Key，并取得业务空间专属 API Host。
2. 按 `server/bailian-proxy/README.md` 部署安全中转，把 `DASHSCOPE_API_KEY` 等真实值放入云端环境变量。
3. 为 `SAY01_ACCESS_TOKEN` 生成一段至少 24 位的随机访问口令；它可以随时在云端更换。
4. 回到 SAY/01，进入任意场景，点最下方荧光粉边框的 `AI LIVE`。
5. 填入中转服务的 HTTPS 地址和访问口令，阅读并勾选联网说明，然后进入场景。

手机只保存中转地址和可撤销访问口令，不保存百炼 API Key；它们不会进入学习进度备份。不要把真实 API Key 发到聊天、截图、GitHub 或 APK 中。

## 怎么练

- 进入已经学过或正在学的场景，点 `AI LIVE`。
- 每回合只说一句。不会完整句时，先说两三个词也可以。
- 可以打字；设备支持时也可点“说给 AI 听”，先确认识别文字，再发送。
- AI 每次只指出一处最值得改的地方。能听懂时不会为了挑错而硬讲语法。
- 想不到下一句时点“给我一点提示”；提示默认隐藏，先给自己一次主动回想的机会。
- 一轮最多 8 回合。结束后系统自动挑一句放进“复习 → AI 回声”，按照 1、3、7、14、30 天逐步再遇见。
- 旧的 AI 记忆句也会在后续场景中作为背景目标，让 AI 在合适的时候自然把它带回来。

## 声音

- 固定课程、拼写词和原角色扮演继续使用随 App 打包的少女声，不依赖百炼。
- AI 开场和动态回复会请求百炼 Mia 女声。音频加载完成后会尝试自动播放；若浏览器拦截自动播放，点回复下方的“听 Mia 少女声”。
- 如果服务端没有配置 `DASHSCOPE_TTS_URL`，文字陪练仍可用，按钮会明确显示“少女声尚未配置”。
- 语音失败时只显示文字，不调用手机系统语音，因此不会回退成不可控男声。
- 当前音色是合成音，不是真人或外教录音；“甜、自然、有情绪”仍需用户在同一台真机试听后判断。

## 隐私和费用

- AI 模式需要联网。本轮文字会经自己的中转发送给阿里云百炼；启用动态语音时，AI 英文回复也会发送给百炼语音服务。
- 中转只记录模型、Token/字符数量和错误码，不记录用户对话原文；手机学习状态仍保存在本机。
- 不要输入身份证、地址、账号、公司秘密或其他个人敏感信息。
- 基础课程永久免费且可离线使用。百炼新人额度有期限，模型与语音服务之后可能按量计费，不能承诺“永久免费无限 AI”。
- 文字价格、免费额度和语音计费均以部署当天的阿里云百炼控制台为准，并应设置预算与费用告警。

## 当前未验证项

- 尚未用用户自己的真实百炼 Key 调用，因此真实回复质量、延迟、Token 成本和 TTS 音色仍待部署测试。
- Android WebView 的跨域请求、自动播放、麦克风听写与 8 回合完整流程仍需真机验收。
- 100 万 Token 等新人额度并非永久免费承诺，且语音额度与文字额度不能混为一项。
